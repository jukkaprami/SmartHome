<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="//unpkg.com/brain.js"></script>
</head>
<body>
    <h1>Brain.js-neurovekko</h1>
<div id="data"></div>

<script>
  // Read learning Data
  const weatherData = {{weatherAndTimeData}}
  const priceData = {{sähkönhintadata}}

  // read data for predictions
  const weatherForecast = {{weatherForecastAndTime}}
</script>

<script>
// Create a Neural Network
const network = new brain.NeuralNetwork();

// Train the Network with 4 input objects
network.train([
  {input:[0,0], output:[0]},
  {input:[0,1], output:[0.2]},
  {input:[1,0], output:[0.5]},
  {input:[1,1], output:[1]},
]);

// Test with true and false ie. [1,0]
let result = network.run([1,0]);

// Display the probability 
document.getElementById("data").innerHTML = result[0];
</script>
</body>
</html>